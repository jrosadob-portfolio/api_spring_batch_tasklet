spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring-batch
    username: root
    password: uOuD0W671Z4aTsHsoFIo
    driver-class-name: com.mysql.cj.jdbc.Driver
  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: always
  jpa:
    hibernate:
      ddl-auto: update

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

